{
  "name": "builder",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/builder",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project builder --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/builder",
        "command": "npx next build --webpack"
      }
    }
  }
}
